@model recruitment.Models.UserApplication

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UserApplication</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CVId" class="control-label"></label>
                <select asp-for="CVId" class ="form-control" asp-items="ViewBag.CVId"></select>
            </div>
            <div class="form-group">
                <label asp-for="JobListingId" class="control-label"></label>
                <select asp-for="JobListingId" class ="form-control" asp-items="ViewBag.JobListingId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ApplicationStatusId" class="control-label"></label>
                <select asp-for="ApplicationStatusId" class ="form-control" asp-items="ViewBag.ApplicationStatusId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
